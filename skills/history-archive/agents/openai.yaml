interface:
  display_name: "History Archive"
  short_description: "Append structured thread history records"
  default_prompt: "Use $history-archive to append an index record and a detailed session archive under .agents/history/{thread_key}/ after a code, docs, config, or policy update."
