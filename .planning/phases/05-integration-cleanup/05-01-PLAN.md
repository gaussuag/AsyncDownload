---
phase: 05-integration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/performance/acceptance.py (DELETED)
  - src/core/models.hpp (verified clean)
  - src/download/download_engine.cpp (verified clean)
autonomous: true
requirements:
  - CLEN-01
  - CLEN-02

must_haves:
  truths:
    - acceptance.py is deleted from scripts/performance/
    - No diagnostic export paths remain that were used by acceptance.py
    - SessionState contains no deprecated metric calculation fields
    - download_engine uses TelemetrySession for all metric recording
  artifacts:
    - path: "scripts/performance/acceptance.py"
      exists: false
    - path: "src/core/models.hpp"
      verified: "No performance_metrics field in SessionState"
    - path: "src/download/download_engine.cpp"
      verified: "Uses record_first_byte_received() via TelemetrySession, no direct metric calculation"
---

<objective>
Delete acceptance.py and verify deprecated metric calculation code has been removed from SessionState and download_engine.
</objective>

<context>
@.planning/phases/05-integration-cleanup/05-CONTEXT.md
@src/core/models.hpp
@src/download/download_engine.cpp
</context>

<interfaces>
SessionState (from models.hpp lines 151-178):
- Has: paths, url, etag, last_modified, options, total_size, accept_ranges, resumed
- Has: downloaded_bytes, persisted_bytes, vdl_offset, queued_packets, queued_bytes (operational state)
- Has: telemetry_session_ (TelemetrySession instance)
- Does NOT have: performance_metrics, first_network_byte_at, progress_rate caches

download_engine.cpp record_first_network_byte (lines 154-161):
- Delegates to session.telemetry_session_.record_first_byte_received()
- No direct metric calculation

download_engine.cpp invoke_progress (lines 163+):
- Gets snapshot from session.telemetry_session_.current_snapshot()
- Populates additional runtime state (total_size, vdl_offset, queued_packets, resumed)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete acceptance.py</name>
  <files>scripts/performance/acceptance.py</files>
  <action>
    Delete the file scripts/performance/acceptance.py.

    This file was a redundant performance acceptance test script that duplicated functionality now covered by benchmark.py and profiler.py. Per D-09, D-10, D-11:
    - acceptance.py at scripts/performance/acceptance.py is marked for deletion
    - All diagnostic export paths referenced by acceptance.py should be removed if not used elsewhere
    - No backup needed — code is in git history

    After deletion, verify:
    - scripts/performance/acceptance.py no longer exists
    - benchmark.py and profiler.py still function (they import from performance_common.py, not acceptance.py)
  </action>
  <verify>
    <automated>test ! -f scripts/performance/acceptance.py && echo "DELETED" || echo "STILL EXISTS"</automated>
  </verify>
  <done>acceptance.py deleted, benchmark.py and profiler.py remain functional</done>
</task>

<task type="auto">
  <name>Task 2: Verify SessionState has no deprecated metric fields</name>
  <files>src/core/models.hpp</files>
  <action>
    Inspect src/core/models.hpp SessionState struct (lines 151-178).

    Verify SessionState does NOT contain:
    - performance_metrics field
    - first_network_byte_at field  
    - Any progress_rate cache fields
    - Any deprecated telemetry-only fields that were removed in Phase 4

    SessionState SHOULD contain:
    - telemetry_session_ (TelemetrySession instance) - this is correct
    - downloaded_bytes, persisted_bytes, vdl_offset, queued_packets, queued_bytes - operational counters, NOT deprecated metric code
    - progress_callback - correct

    Per D-01 through D-05: Only metric calculation related code is deprecated. Operational state needed for download coordination (downloaded_bytes, persisted_bytes, vdl_offset, etc.) is NOT deprecated.
  </action>
  <verify>
    <automated>grep -n "performance_metrics\|first_network_byte_at\|progress_rate" src/core/models.hpp || echo "VERIFIED_CLEAN"</automated>
  </verify>
  <done>SessionState verified: no deprecated metric fields, only operational state and telemetry_session_</done>
</task>

<task type="auto">
  <name>Task 3: Verify download_engine uses TelemetrySession correctly</name>
  <files>src/download/download_engine.cpp</files>
  <action>
    Inspect src/download/download_engine.cpp for any deprecated direct metric calculation.

    Verify that record_first_network_byte (lines 154-161):
    - Calls session.telemetry_session_.record_first_byte_received()
    - Does NOT calculate time_to_first_byte_ms directly

    Verify that invoke_progress (lines 163+):
    - Gets snapshot from session.telemetry_session_.current_snapshot()
    - Does NOT calculate network_speed, disk_speed, or TTFB directly
    - Populates only runtime coordination state (total_size, vdl_offset, queued_packets, resumed, active_ranges, etc.)

    Per D-04: download_engine uses record_first_byte_received() via TelemetrySession, not direct calculation.
    Per MIGR-04: download_engine no longer calculates speeds, TTFB, or summary fields directly.
  </action>
  <verify>
    <automated>grep -n "time_to_first_byte\|avg_network_speed\|avg_disk_speed\|first_network_byte_at" src/download/download_engine.cpp | grep -v "record_first_byte\|telemetry_session" || echo "VERIFIED_CLEAN"</automated>
  </verify>
  <done>download_engine verified: uses TelemetrySession for all metric recording, no direct calculation</done>
</task>

</tasks>

<verification>
CLEN-01 verification:
- scripts/performance/acceptance.py does not exist

CLEN-02 verification:
- grep for "performance_metrics" in models.hpp returns empty
- grep for direct metric calculations in download_engine.cpp returns empty
- SessionState contains only telemetry_session_ (correct) and operational counters
</verification>

<success_criteria>
- acceptance.py deleted
- No deprecated metric calculation fields in SessionState
- download_engine uses TelemetrySession exclusively for metrics
</success_criteria>

<output>
After completion, create .planning/phases/05-integration-cleanup/05-01-SUMMARY.md
</output>
