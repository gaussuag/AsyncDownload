---
phase: 05-integration-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/performance/benchmark.py
  - scripts/performance/profiler.py
autonomous: true
requirements:
  - INTG-03
  - INTG-04

must_haves:
  truths:
    - benchmark.py completes without error and produces identical output
    - profiler.py completes without error and produces identical output
    - PerformanceSummary fields match expected values before and after refactoring
  artifacts:
    - path: "scripts/performance/benchmark.py"
      verified: "Runs successfully, output format unchanged"
    - path: "scripts/performance/profiler.py"
      verified: "Runs successfully, output format unchanged"
---

<objective>
Run smoke tests for benchmark.py and profiler.py to verify backward compatibility. Both scripts must pass with identical output after the Phase 4 telemetry refactoring.
</objective>

<context>
@.planning/phases/05-integration-cleanup/05-CONTEXT.md
@scripts/performance/benchmark.py
@scripts/performance/profiler.py
</context>

<interfaces>
benchmark.py:
- Runs downloads with various configurations
- Outputs CSV and markdown report
- Uses performance_common.py for shared functionality
- Exit code 0 = success

profiler.py:
- Runs downloads with Windows Performance Toolkit
- Outputs ETL traces and CSV
- Uses performance_common.py for shared functionality  
- Exit code 0 = success

Per D-06, D-07, D-08:
- "Identical" means: same field values in PerformanceSummary, same file structure, same exit codes
- Smoke test passes if: benchmark.py completes without error, profiler.py completes without error
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run benchmark.py smoke test</name>
  <read_first>
    scripts/performance/benchmark.py
  </read_first>
  <action>
    Run benchmark.py with a simple test configuration to verify backward compatibility.

    Build the project first:
    ```
    scripts\build.bat
    ```

    Run benchmark.py with minimal configuration:
    ```
    python scripts/performance/benchmark.py --url http://speedtest.tele2.net/1MB.zip --exe build/src/Debug/AsyncDownload.exe --repeats 1 --case-list baseline_default
    ```

    Expected behavior:
    - Script completes with exit code 0
    - CSV output contains expected columns (avg_network_speed, avg_disk_speed, time_to_first_byte_ms, etc.)
    - No crashes or assertion failures

    Per INTG-03: benchmark.py smoke test passes with identical output
    Per D-06: Output must be identical to pre-refactoring output
  </action>
  <verify>
    <automated>cd /d D:\git_repository\coding_with_agents\AsyncDownload && python scripts/performance/benchmark.py --help > nul 2>&1 && echo "BENCHMARK_OK"</automated>
  </verify>
  <done>benchmark.py runs successfully and produces expected output format</done>
</task>

<task type="auto">
  <name>Task 2: Run profiler.py smoke test</name>
  <read_first>
    scripts/performance/profiler.py
  </read_first>
  <action>
    Run profiler.py to verify it still functions correctly after refactoring.

    Note: profiler.py uses Windows Performance Toolkit (WPT) which may not be installed on all systems.

    Run profiler.py with --help to verify the script is functional:
    ```
    python scripts/performance/profiler.py --help
    ```

    If WPT is available, run a minimal profile:
    ```
    python scripts/performance/profiler.py --url http://speedtest.tele2.net/1MB.zip --exe build/src/Debug/AsyncDownload.exe --repeats 1
    ```

    Per INTG-04: profiler.py smoke test passes with identical output
    Per D-06: Output must be identical to pre-refactoring output
  </action>
  <verify>
    <automated>cd /d D:\git_repository\coding_with_agents\AsyncDownload && python scripts/performance/profiler.py --help > nul 2>&1 && echo "PROFILER_OK"</automated>
  </verify>
  <done>profiler.py script is functional and produces expected output</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify smoke test output matches expected values</name>
  <action>
    Manually verify the smoke test output.

    After running the benchmark.py smoke test in Task 1:
    1. Check that PerformanceSummary fields are populated correctly
    2. Verify CSV output has expected columns:
       - avg_network_speed_mb_s
       - avg_disk_speed_mb_s  
       - time_to_first_byte_ms
       - max_memory_bytes
       - etc.
    3. Check that no fields are missing or malformed

    After running the profiler.py smoke test in Task 2 (if WPT available):
    1. Check that ETL traces are generated
    2. Verify CSV output has expected columns

    Per D-07: "Identical" means same field values in PerformanceSummary
  </action>
  <verify>
    <automated>echo "Manual verification required - run benchmark.py and check output"</automated>
  </verify>
  <done>Smoke tests pass with output matching pre-refactoring behavior</done>
</task>

</tasks>

<verification>
INTG-03: benchmark.py completes without error, exit code 0
INTG-04: profiler.py completes without error, exit code 0
Both: PerformanceSummary fields match expected values
</verification>

<success_criteria>
- benchmark.py smoke test passes (exit code 0)
- profiler.py smoke test passes (exit code 0 or graceful WPT-not-found message)
- Output fields are identical to pre-refactoring baseline
</success_criteria>

<output>
After completion, create .planning/phases/05-integration-cleanup/05-02-SUMMARY.md
</output>
