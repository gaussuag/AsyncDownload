---
phase: 01-telemetry-skeleton
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - include/asyncdownload/telemetry/telemetry_collector.hpp
  - src/telemetry/telemetry_collector.cpp
autonomous: true
requirements:
  - TELE-04
user_setup: []
must_haves:
  truths:
    - "TelemetryCollector can start a background consumer loop and stop cleanly."
    - "Collector query methods return stable stub values without metric computation in Phase 1."
  artifacts:
    - path: include/asyncdownload/telemetry/telemetry_collector.hpp
      provides: Collector public interface
      contains: "class TelemetryCollector"
    - path: src/telemetry/telemetry_collector.cpp
      provides: Background consumer implementation
      min_lines: 30
  key_links:
    - from: include/asyncdownload/telemetry/telemetry_collector.hpp
      to: include/asyncdownload/telemetry/telemetry_sink.hpp
      via: sink ownership or reference
      pattern: "TelemetrySink"
---

<objective>
Implement the Phase 1 collector stub that drains events in the background.

Purpose: The session facade needs a consumer endpoint now, even though full metric computation is deferred to Phase 2.
Output: Collector API, worker-thread lifecycle, and stub snapshot/summary queries.
</objective>

<execution_context>
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/workflows/execute-plan.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@include/asyncdownload/types.hpp
@include/asyncdownload/telemetry/telemetry_sink.hpp
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define TelemetryCollector lifecycle and query surface</name>
  <files>include/asyncdownload/telemetry/telemetry_collector.hpp</files>
  <read_first>include/asyncdownload/types.hpp, include/asyncdownload/telemetry/telemetry_sink.hpp</read_first>
  <action>Define TelemetryCollector with start_consuming(), stop_consuming(), wait_until_drained(), current_snapshot(), and final_summary(). Use ProgressSnapshot and PerformanceSummary as the stub query result types to preserve the future public contract.</action>
  <verify>The header provides a complete interface for TelemetrySession and tests.</verify>
  <acceptance_criteria>
    - TelemetryCollector query methods return ProgressSnapshot and PerformanceSummary
    - Lifecycle API includes explicit start and stop methods
  </acceptance_criteria>
  <done>The collector contract is defined and aligns with Phase 1 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Implement background drain loop and graceful shutdown</name>
  <files>src/telemetry/telemetry_collector.cpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_collector.hpp, include/asyncdownload/telemetry/telemetry_sink.hpp</read_first>
  <action>Implement a background consumer thread that drains the sink until stop is requested. Use a timed dequeue loop so shutdown can observe the stop flag without injecting control events. Keep event processing as a stub that records no metrics yet but safely drains the queue and preserves FIFO consumption order.</action>
  <verify>Collector can be started, drained, stopped, and destroyed without deadlock.</verify>
  <acceptance_criteria>
    - stop_consuming joins the worker thread safely
    - wait_until_drained waits until queue backlog reaches zero
    - current_snapshot and final_summary return well-defined default values in Phase 1
  </acceptance_criteria>
  <done>The collector thread model is established for later metric computation work.</done>
</task>
</tasks>

<verification>
- [ ] scripts\build.bat succeeds
- [ ] Collector start/stop can be exercised from unit tests without hangs
</verification>

<success_criteria>
- All tasks completed
- TELE-04 collector lifecycle exists and is thread-safe
- Phase 1 keeps metric computation deferred while draining events correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-skeleton/01-04-SUMMARY.md`
</output>
