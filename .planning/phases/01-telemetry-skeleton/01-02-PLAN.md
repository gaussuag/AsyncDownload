---
phase: 01-telemetry-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - include/asyncdownload/telemetry/telemetry_event.hpp
autonomous: true
requirements:
  - TELE-01
  - TELE-02
  - TELE-06
user_setup: []
must_haves:
  truths:
    - "All seven telemetry event types are defined in a stable public enum."
    - "Telemetry events carry producer-side steady_clock timestamps in uint64 nanoseconds."
    - "Telemetry payload storage stays fixed-size and heap-free."
  artifacts:
    - path: include/asyncdownload/telemetry/telemetry_event.hpp
      provides: Telemetry event model and payload storage
      contains: "enum class TelemetryEventType"
  key_links:
    - from: include/asyncdownload/telemetry/telemetry_event.hpp
      to: include/asyncdownload/telemetry/telemetry_session.hpp
      via: shared event construction types
      pattern: "TelemetryEvent"
---

<objective>
Define the event and payload types that every telemetry producer and consumer will share.

Purpose: The event model is the contract between producer code, queue transport, collector consumption, and future metric computation.
Output: Public telemetry event enums, payload structs, tagged storage, and timestamp helpers.
</objective>

<execution_context>
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/workflows/execute-plan.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-telemetry-skeleton/01-CONTEXT.md
@include/asyncdownload/types.hpp
@include/asyncdownload/performance_metrics.hpp
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define public telemetry event enums and payload structs</name>
  <files>include/asyncdownload/telemetry/telemetry_event.hpp</files>
  <read_first>.planning/phases/01-telemetry-skeleton/01-CONTEXT.md, include/asyncdownload/types.hpp</read_first>
  <action>Create TelemetryEventType with TaskStarted, FirstByteReceived, DownloadDelta, PersistDelta, QueuePaused, MemorySample, and TaskCompleted. Define compact payload structs for each event and a pause-reason enum suitable for fixed-size storage.</action>
  <verify>Header compiles in both library and tests.</verify>
  <acceptance_criteria>
    - telemetry_event.hpp defines all seven event types
    - QueuePaused payload stores reason and queue-full state without std::string or std::vector
    - TaskCompleted payload stores a compact completion status field
  </acceptance_criteria>
  <done>The event taxonomy and payload records are complete and public.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixed-size payload carrier and timestamp helpers</name>
  <files>include/asyncdownload/telemetry/telemetry_event.hpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_event.hpp</read_first>
  <action>Represent payload storage as a fixed-size tagged union or equivalent variant-free storage with static_assert-based size checks. Define TelemetryEvent with uint64 timestamp_ns and provide a helper that converts steady_clock::time_point to nanoseconds for producer-side emission.</action>
  <verify>Static assertions enforce fixed-size constraints and no allocation-bearing members are introduced.</verify>
  <acceptance_criteria>
    - TelemetryEvent contains type, timestamp_ns, and payload
    - timestamp_ns uses std::uint64_t
    - telemetry_event.hpp contains static_assert checks for payload and event sizes
  </acceptance_criteria>
  <done>Telemetry events are fixed-size, timestamped, and ready for transport.</done>
</task>
</tasks>

<verification>
- [ ] scripts\build.bat succeeds
- [ ] telemetry_event.hpp contains static_assert guards for payload sizing
</verification>

<success_criteria>
- All tasks completed
- Event definitions satisfy TELE-01, TELE-02, and TELE-06
- No dynamic allocation is required by event creation
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-skeleton/01-02-SUMMARY.md`
</output>
