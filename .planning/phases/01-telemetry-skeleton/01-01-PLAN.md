---
phase: 01-telemetry-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/asyncdownload/telemetry.hpp
  - src/CMakeLists.txt
autonomous: true
requirements:
  - TELE-03
  - TELE-04
  - TELE-05
user_setup: []
must_haves:
  truths:
    - "Telemetry public headers are exposed through asyncdownload/telemetry.hpp."
    - "Telemetry implementation sources build as part of the asyncdownload library."
  artifacts:
    - path: include/asyncdownload/telemetry.hpp
      provides: Telemetry public header aggregation
      contains: '#include "asyncdownload/telemetry/telemetry_event.hpp"'
    - path: src/CMakeLists.txt
      provides: Telemetry source build integration
      contains: "file(GLOB_RECURSE ASYNCDOWNLOAD_LIBRARY_SOURCES CONFIGURE_DEPENDS"
  key_links:
    - from: include/asyncdownload/telemetry.hpp
      to: include/asyncdownload/telemetry/telemetry_session.hpp
      via: aggregate include
      pattern: 'telemetry_session\\.hpp'
---

<objective>
Create the public telemetry module skeleton and make sure the build system sees it.

Purpose: The rest of the telemetry plans need a stable public include path and source layout before they can compile.
Output: The aggregate public header and source-tree build integration for src/telemetry.
</objective>

<execution_context>
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/workflows/execute-plan.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-telemetry-skeleton/01-CONTEXT.md
@src/CMakeLists.txt
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create telemetry public aggregation header</name>
  <files>include/asyncdownload/telemetry.hpp</files>
  <read_first>.planning/phases/01-telemetry-skeleton/01-CONTEXT.md, include/asyncdownload/client.hpp</read_first>
  <action>Create include/asyncdownload/telemetry.hpp and re-export telemetry_event.hpp, telemetry_sink.hpp, telemetry_collector.hpp, and telemetry_session.hpp from the asyncdownload public include tree.</action>
  <verify>Header compiles when included by a translation unit in the test target.</verify>
  <acceptance_criteria>
    - include/asyncdownload/telemetry.hpp exists
    - include/asyncdownload/telemetry.hpp includes all four telemetry component headers
  </acceptance_criteria>
  <done>Telemetry public aggregation header exists and is usable from public include paths.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm build picks up telemetry sources</name>
  <files>src/CMakeLists.txt</files>
  <read_first>src/CMakeLists.txt</read_first>
  <action>Ensure the asyncdownload library build configuration will compile new src/telemetry/*.cpp files without needing extra per-file registration and without breaking the existing executable and tests.</action>
  <verify>Configure and build succeed after telemetry source files are added.</verify>
  <acceptance_criteria>
    - src/CMakeLists.txt still glob-loads asyncdownload library sources recursively
    - Telemetry source files under src/telemetry are part of the asyncdownload static library
  </acceptance_criteria>
  <done>The build graph includes telemetry sources and remains compatible with the existing targets.</done>
</task>
</tasks>

<verification>
- [ ] scripts\build.bat succeeds after telemetry files are added
- [ ] Public code can include asyncdownload/telemetry.hpp without missing-header errors
</verification>

<success_criteria>
- All tasks completed
- Telemetry directory structure is established for later plans
- The build remains green after new telemetry files are introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-skeleton/01-01-SUMMARY.md`
</output>
