---
phase: 01-telemetry-skeleton
plan: 06
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - tests/telemetry_skeleton_test.cpp
autonomous: true
requirements:
  - TELE-01
  - TELE-02
  - TELE-03
  - TELE-04
  - TELE-05
  - TELE-06
user_setup: []
must_haves:
  truths:
    - "Telemetry skeleton behavior is covered by focused unit tests."
    - "The sink, collector, and session lifecycle work without deadlocks or missing event types."
  artifacts:
    - path: tests/telemetry_skeleton_test.cpp
      provides: Telemetry skeleton test coverage
      min_lines: 40
      contains: "TelemetrySkeleton"
  key_links:
    - from: tests/telemetry_skeleton_test.cpp
      to: include/asyncdownload/telemetry/telemetry_session.hpp
      via: unit test includes
      pattern: "telemetry_session\\.hpp"
---

<objective>
Add executable coverage for the telemetry skeleton.

Purpose: Phase 1 needs proof that the public event model, queue transport, collector lifecycle, and session facade compile and behave as intended before Phase 2 builds on them.
Output: A focused GoogleTest suite for TelemetryEvent, TelemetrySink, TelemetryCollector, and TelemetrySession.
</objective>

<execution_context>
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/workflows/execute-plan.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/templates/summary.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/main_test.cpp
@include/asyncdownload/telemetry.hpp
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add structural tests for telemetry event and sink behavior</name>
  <files>tests/telemetry_skeleton_test.cpp</files>
  <read_first>tests/main_test.cpp, include/asyncdownload/telemetry/telemetry_event.hpp, include/asyncdownload/telemetry/telemetry_sink.hpp</read_first>
  <action>Create GoogleTest cases that verify all event types can be represented, payload/event sizes stay fixed, and TelemetrySink preserves FIFO dequeue behavior for emitted events. Include at least one timed dequeue or try_dequeue empty-queue assertion.</action>
  <verify>AsyncDownload_tests.exe --gtest_filter=TelemetrySkeleton* passes.</verify>
  <acceptance_criteria>
    - telemetry_skeleton_test.cpp includes TelemetrySkeleton-prefixed tests
    - Tests cover event enum presence and sink queue behavior
  </acceptance_criteria>
  <done>Telemetry event model and sink behavior are covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add collector and session lifecycle tests</name>
  <files>tests/telemetry_skeleton_test.cpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_collector.hpp, include/asyncdownload/telemetry/telemetry_session.hpp</read_first>
  <action>Add tests that start and stop a TelemetryCollector safely, and that TelemetrySession record_* methods emit the expected event types and can be destroyed cleanly after activity.</action>
  <verify>AsyncDownload_tests.exe --gtest_filter=TelemetrySkeleton* passes with the new cases.</verify>
  <acceptance_criteria>
    - Tests cover collector lifecycle
    - Tests cover session event emission for multiple record_* methods
    - Tests do not rely on sleeps longer than needed for timed queue polling
  </acceptance_criteria>
  <done>Collector and session behavior are covered by repeatable unit tests.</done>
</task>
</tasks>

<verification>
- [ ] scripts\build.bat succeeds
- [ ] build\tests\Debug\AsyncDownload_tests.exe --gtest_filter=TelemetrySkeleton* passes
</verification>

<success_criteria>
- All tasks completed
- Phase 1 telemetry skeleton has dedicated unit coverage
- Tests validate the public contracts needed by later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-skeleton/01-06-SUMMARY.md`
</output>
