---
phase: 01-telemetry-skeleton
plan: 05
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - include/asyncdownload/telemetry/telemetry_session.hpp
  - src/telemetry/telemetry_session.cpp
autonomous: true
requirements:
  - TELE-05
  - TELE-06
user_setup: []
must_haves:
  truths:
    - "TelemetrySession is the public facade that records all Phase 1 telemetry event types."
    - "Every record_* call stamps the event at emission time using steady_clock."
    - "Session query methods delegate to the collector rather than storing metrics state locally."
  artifacts:
    - path: include/asyncdownload/telemetry/telemetry_session.hpp
      provides: Session facade interface
      contains: "class TelemetrySession"
    - path: src/telemetry/telemetry_session.cpp
      provides: Facade implementation
      min_lines: 30
  key_links:
    - from: src/telemetry/telemetry_session.cpp
      to: include/asyncdownload/telemetry/telemetry_event.hpp
      via: event construction
      pattern: "steady_clock"
---

<objective>
Implement the public facade that producers will call to emit telemetry events.

Purpose: Business code in later phases must depend only on TelemetrySession rather than on queue or collector details.
Output: TelemetrySession header and implementation with record_* and query methods.
</objective>

<execution_context>
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/workflows/execute-plan.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@include/asyncdownload/telemetry/telemetry_event.hpp
@include/asyncdownload/telemetry/telemetry_sink.hpp
@include/asyncdownload/telemetry/telemetry_collector.hpp
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define TelemetrySession public API</name>
  <files>include/asyncdownload/telemetry/telemetry_session.hpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_event.hpp, include/asyncdownload/types.hpp</read_first>
  <action>Define TelemetrySession as the owning facade for a TelemetrySink and TelemetryCollector. Expose record_task_started(), record_first_byte_received(), record_download_delta(std::uint64_t), record_persist_delta(std::uint64_t), record_pause(TelemetryPauseReason, bool), record_memory_sample(std::uint64_t), record_task_completed(), current_snapshot(), and final_summary().</action>
  <verify>The header is sufficient for future download_engine integration without exposing collector internals.</verify>
  <acceptance_criteria>
    - TelemetrySession exposes all required Phase 1 record methods
    - Query methods return ProgressSnapshot and PerformanceSummary
  </acceptance_criteria>
  <done>The public session facade surface matches the phase context decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement producer-side timestamped event emission</name>
  <files>src/telemetry/telemetry_session.cpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_session.hpp, include/asyncdownload/telemetry/telemetry_event.hpp</read_first>
  <action>Implement TelemetrySession so each record_* method captures a steady_clock timestamp immediately before enqueue, fills the correct payload variant, and sends the event through the sink. Start the collector automatically for a live session, and delegate current_snapshot()/final_summary() to the collector.</action>
  <verify>Record methods can be called from tests and result in queue-visible events with increasing timestamps.</verify>
  <acceptance_criteria>
    - Every record_* method constructs a TelemetryEvent
    - Event timestamps are derived from steady_clock at call time
    - Session destruction stops the collector cleanly
  </acceptance_criteria>
  <done>The session facade can emit and expose telemetry without local metrics state.</done>
</task>
</tasks>

<verification>
- [ ] scripts\build.bat succeeds
- [ ] TelemetrySession tests can observe correct event types after record_* calls
</verification>

<success_criteria>
- All tasks completed
- TELE-05 and TELE-06 are satisfied for the public facade
- TelemetrySession is ready for future integration into download_engine and persistence_thread
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-skeleton/01-05-SUMMARY.md`
</output>
