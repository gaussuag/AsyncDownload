---
phase: 01-telemetry-skeleton
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - include/asyncdownload/telemetry/telemetry_sink.hpp
  - src/telemetry/telemetry_sink.cpp
autonomous: true
requirements:
  - TELE-03
user_setup: []
must_haves:
  truths:
    - "TelemetrySink provides multi-producer enqueueing on top of moodycamel::BlockingConcurrentQueue."
    - "TelemetryCollector can block on the sink without reaching into queue internals directly."
  artifacts:
    - path: include/asyncdownload/telemetry/telemetry_sink.hpp
      provides: Public sink interface
      contains: "class TelemetrySink"
    - path: src/telemetry/telemetry_sink.cpp
      provides: Queue-backed sink implementation
      min_lines: 20
  key_links:
    - from: include/asyncdownload/telemetry/telemetry_sink.hpp
      to: libs/concurrentqueue/include/concurrentqueue/blockingconcurrentqueue.h
      via: queue wrapper
      pattern: "BlockingConcurrentQueue"
---

<objective>
Implement the queue-backed transport layer for telemetry events.

Purpose: Producers need a single safe enqueue API while the collector needs a blocking consumer API that stays isolated behind project types.
Output: Public TelemetrySink interface and queue-backed implementation in src/telemetry.
</objective>

<execution_context>
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/workflows/execute-plan.md
@D:/git_repository/coding_with_agents/AsyncDownload/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-telemetry-skeleton/01-CONTEXT.md
@include/asyncdownload/telemetry/telemetry_event.hpp
@libs/concurrentqueue/include/concurrentqueue/blockingconcurrentqueue.h
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define TelemetrySink public API</name>
  <files>include/asyncdownload/telemetry/telemetry_sink.hpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_event.hpp, libs/concurrentqueue/include/concurrentqueue/blockingconcurrentqueue.h</read_first>
  <action>Expose TelemetrySink with default constructor, enqueue(event), enqueue_from_producer(token, event), wait_dequeue(event), wait_dequeue_timed(event, timeout), try_dequeue(event), and size_approx() helpers. Re-export the queue producer token type so callers can opt into moodycamel producer tokens when needed.</action>
  <verify>The header is sufficient for TelemetryCollector and TelemetrySession to consume without seeing queue internals.</verify>
  <acceptance_criteria>
    - TelemetrySink exposes producer-token aware enqueueing
    - TelemetrySink exposes blocking and non-blocking dequeue APIs
    - Public API uses TelemetryEvent rather than raw queue types
  </acceptance_criteria>
  <done>The sink API is stable for producers and collector code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement queue-backed sink behavior</name>
  <files>src/telemetry/telemetry_sink.cpp</files>
  <read_first>include/asyncdownload/telemetry/telemetry_sink.hpp, libs/concurrentqueue/include/concurrentqueue/blockingconcurrentqueue.h</read_first>
  <action>Back TelemetrySink with moodycamel::BlockingConcurrentQueue<TelemetryEvent>. Implement method bodies using verified queue APIs from the vendored header, keeping all operations noexcept-compatible where practical.</action>
  <verify>Single-translation-unit build succeeds and timed dequeue compiles with std::chrono inputs.</verify>
  <acceptance_criteria>
    - enqueue and enqueue_from_producer forward to moodycamel queue calls
    - wait_dequeue and wait_dequeue_timed use blocking queue methods
    - try_dequeue reports false when the queue is empty
  </acceptance_criteria>
  <done>The sink moves telemetry events between producers and the collector thread safely.</done>
</task>
</tasks>

<verification>
- [ ] scripts\build.bat succeeds
- [ ] TelemetrySink methods compile against the vendored concurrentqueue headers
</verification>

<success_criteria>
- All tasks completed
- TELE-03 is implemented with validated third-party queue semantics
- Collector-facing dequeue APIs are ready for the next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-skeleton/01-03-SUMMARY.md`
</output>
